<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lego Collection - Set Details</title>
  <link rel="stylesheet" href="/css/main.css">
</head>

<body tabindex="-1">
    <%- include('partials/navbar', { page: "" }) %>
  
  <br>

  <div class="container mx-auto p-4">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center ">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold"><%= set.name %></h1>
          <p class="py-6">Below, you will find detailed information about the set:</p>
        </div>
      </div>
    </div>
  
    <br />
  
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <img class="w-full rounded-lg" src="<%= set.img_url %>" alt="<%= set.name %>" />
      </div>
      <div class="prose">
        <p>
          <span class="font-bold">Name:</span> <%= set.name %> <br />
          <span class="font-bold">Year:</span> <%= set.year %> <br />
          <span class="font-bold">Theme:</span> <%= set.Theme.name %> <br />
          <span class="font-bold">Parts:</span> <%= set.num_parts %> <br>
          <div id="quote" class="py-3"></div>
          <br />
          <a class="btn btn-success btn-sm" href="/lego/editSet/028-1">EDIT SET: <%= set.name %></a>
          <a class="btn btn-primary btn-sm" href="#" onclick="history.back(); return false;">RETURN</a>
        </p>
      </div>
    </div>
  
  </div>
  

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      fetch("https://quotable.io/random")
        .then(response => response.json())
        .then(data => {
          const quoteElement = document.getElementById("quote");
          quoteElement.innerHTML = `<blockquote>"${data.content}" - ${data.author}</blockquote>`;
        })
        .catch(error => console.error("Error fetching quote:", error));
    });
  </script>

<footer class="bg-base-100 text-center py-4">
  <p class="text-sm">
      I have used this website as reference:
      <br>
      <a href="https://rose-camel-belt.cyclic.app/" target="_blank" rel="noopener noreferrer" class="text-primary">
        <u>https://rose-camel-belt.cyclic.app/</u>
        
      </a>
    </p>
</footer>
<br>
</body>

</html>
